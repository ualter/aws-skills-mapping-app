---
version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14.x
  pre_build:
    on-failure: ABORT
    commands:
      - echo Start pre_build...
      - npm install -g @angular/cli@latest
      
  build:
    on-failure: ABORT
    commands:
      - echo Build started on `date`
      - ng build
      
  post_build:
    on-failure: ABORT
    commands:
      - echo Start post_build...
      

