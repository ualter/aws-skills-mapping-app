---
version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14.x
  pre_build:
    on-failure: ABORT
    commands:
      - echo Start pre_build...
      
  build:
    on-failure: ABORT
    commands:
      - echo Build started on `date`
      - ng build
      
  post_build:
    on-failure: ABORT
    commands:
      - echo Start post_build...
      

